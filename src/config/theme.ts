import type { MantineThemeOverride } from "@mantine/styles"

export const theme: MantineThemeOverride = {
  colorScheme: "light",
  primaryColor: "orange",
  fontFamily: "DM Sans",
  headings: {
    fontFamily: "DM Sans",
  },
}
